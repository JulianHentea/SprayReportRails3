 <div class="main-area">
	<div class="container">
		<div class="row">
			<div class="span8">
			<h1>All users</h1>
			<%= will_paginate %>
				<div class="all-users-list">
					<ul class="users">
					<%= render @users %>
					</ul>
				</div>
			<%= will_paginate %>
			</div>
		</div>
	</div>
	
	
	<!-- End Main Area -->
	