<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
			
		<p>
		  <%= f.text_field :title, placeholder: "Enter a title..." %>
		</p>
		<p>
    	<%= f.text_area :content, placeholder: "Compose new a new story..." %>
  	</p>
		<div class="field">
		  <%= f.text_field :tag_list, placeholder: "Tags (separated by commas)"%>
		</div>
	</div>
  <%= f.submit "Update your story", class: "btn btn-large btn-primary" %>
<% end %>